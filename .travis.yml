language: c++
compiler: gcc

services:
  - docker

#install:
#  - cd build
#  - cmake ..
#  - make

#before_script:
#  - mvn clean

jobs:
  include:
    - stage: configure
#      install: skip
#      jdk: oraclejdk11
      script:
        - mkdir build
        - cd build
        - cmake ..
    - stage: build
      script: make

stages:
  - configure
  - build